# from django import forms
# from .models import ProjectUsers, only_int
# from django.core.exceptions import ValidationError

# class RegistrationForm(forms.ModelForm):
# 	IIN = forms.CharField(validators=[only_int])
# 	telephone = forms.CharField(validators=[only_int])

# 	class Meta:
# 		model = ProjectUsers
# 		fields = ['IIN', 'full_name', 'position', 'telephone', 'region', 'school', 'auditorya', 'project']

